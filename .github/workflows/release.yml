name: Release

on:
  push:
    branches: [main]

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      python: ${{ steps.changes.outputs.python }}
      javascript: ${{ steps.changes.outputs.javascript }}
      php: ${{ steps.changes.outputs.php }}
      cli: ${{ steps.changes.outputs.cli }}
    steps:
      - uses: actions/checkout@v7

      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            python:
              - 'python/**'
            javascript:
              - 'javascript/**'
            php:
              - 'php/**'
            # The CLI bundles the JavaScript SDK's source, so an SDK release is
            # also a CLI release — otherwise the published CLI keeps shipping
            # the validator from whenever cli/ last changed.
            cli:
              - 'cli/**'
              - 'javascript/**'
              - 'schemas/**'

  publish-python:
    needs: detect-changes
    if: needs.detect-changes.outputs.python == 'true'
    uses: ./.github/workflows/publish-python.yml
    secrets: inherit

  publish-javascript:
    needs: detect-changes
    if: needs.detect-changes.outputs.javascript == 'true'
    uses: ./.github/workflows/publish-javascript.yml
    secrets: inherit

  publish-php:
    needs: detect-changes
    if: needs.detect-changes.outputs.php == 'true'
    uses: ./.github/workflows/publish-php.yml
    secrets: inherit

  # After the SDK, so that a commit touching both publishes a CLI built from the
  # SDK source it was released with rather than racing it.
  publish-cli:
    needs: [detect-changes, publish-javascript]
    if: always() && needs.detect-changes.outputs.cli == 'true' && needs.publish-javascript.result != 'failure'
    uses: ./.github/workflows/publish-cli.yml
    secrets: inherit
